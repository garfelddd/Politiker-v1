import { OnProcessingInputDirective } from './on-processing-input.directive';
import { ElementRef, Renderer } from '@angular/core';

describe('OnProcessingInputDirective', () => {
  it('should create an instance', () => {
  });
});
